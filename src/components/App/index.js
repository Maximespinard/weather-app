import React from 'react';
import { Typography } from '@material-ui/core';

const App = () => (
  <div>
    <Typography variant="h1" component="h2">
      h1. Heading
    </Typography>
  </div>
);

export default App;
